# Database configuration
DB_USER=
DB_PASS=
DB_NAME=
DB_URL=postgresql+psycopg2://${DB_USER}:${DB_PASS}@db/${DB_NAME}
DB_URL_WORKER=postgresql://${DB_USER}:${DB_PASS}@db/${DB_NAME}
VITE_API_BASE_URL=/api
VITE_API_PROXY_TARGET=http://localhost:8000
